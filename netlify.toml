[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  # Do not force this redirect — allow Netlify to serve existing static assets
